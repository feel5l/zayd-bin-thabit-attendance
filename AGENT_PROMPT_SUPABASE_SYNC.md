# الأمر الجاهز — بناء مزامنة Supabase (Sync-1 إلى Sync-4)

**لا ترسله قبل إتمام Gate A في `SUPABASE_SYNC_BUILD_PLAN.md`** (إنشاء مشروع Supabase والحصول على الروابط والمفاتيح). بدونها سيتوقف الوكيل عند أول مهمة.

انسخ ما بين الأسطر:

```text
اعمل داخل C:\Users\Tonjo\Projects\zayd-bin-thabit-attendance فقط.

اقرأ هذين الملفين كاملين قبل أي سطر كود:
  1. SYNC_DESIGN.md — القرارات المعمارية الثابتة (المخطط، قواعد التعارض، مسار
     الاستيراد). لا تُعاد مناقشتها هنا.
  2. SUPABASE_SYNC_BUILD_PLAN.md — خطوات التنفيذ التفصيلية والتزم بقواعده
     تحت "RULES FOR THE EXECUTING AGENT" حرفيًا.

نفّذ PHASE Sync-1 ثم Sync-2 ثم Sync-3 ثم Sync-4 بهذا الترتيب بالضبط، كل
مرحلة كما هي موصوفة في SUPABASE_SYNC_BUILD_PLAN.md مع أوامر VERIFY الخاصة
بها. لا تنتقل لمرحلة قبل أن تنجح كل أوامر VERIFY في المرحلة التي قبلها.

بيانات مشروع Supabase (Gate A) موجودة في .env.local — اقرأها من هناك،
لا تطلبها مني في المحادثة، ولا تكتب service_role key في أي ملف يُتتبَّع
بـ git.

القاعدة التي لا استثناء لها: التطبيق يعمل بكامل وظائفه محليًا حتى بدون
اتصال بـ Supabase إطلاقًا. إن كسر أي تعديل هذا، تراجع عنه قبل المتابعة.

توقف تمامًا بعد Sync-4. لا تنفّذ Sync-5 مهما بدا الأمر جاهزًا — هو ترحيل
بيانات حقيقية لمدرسة فعلية وله موعد أحدده أنا لاحقًا.

بعد كل مرحلة اكتب لي سطرين: ما بُني، ونتيجة VERIFY بالتفصيل (نجح/فشل لكل
بند). إن فشل أي VERIFY، توقف عند تلك المرحلة وأخبرني بالتفصيل بدل محاولة
تجاوزه بنفسك.
```

---

## بعد الانتهاء

راجع أنت شخصيًا، لا الوكيل: أن `service_role key` غير موجود في أي commit (`git log -p --all | grep -i service_role` من جهازك)، وأن `dist/` بعد البناء لا يحوي أرقام جوالات حقيقية.

ثم — وفقط بعد أن ترى المزامنة تعمل فعليًا بين جهازين لعدة أيام اختبار — أكتب لك أمر Sync-5 لنافذة القطع الفعلية.
