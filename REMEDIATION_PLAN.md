# خطة الإصلاح — نظام رصد الغياب (مدرسة زيد بن ثابت)

**الإصدار:** v1.0  
**التاريخ:** 2026-09-02  
**الحالة:** تشخيص مكتمل — بانتظار الموافقة على التنفيذ  
**مرتبط بـ:** تشخيص فشل تحديث الحصص والجدول المدرسي

---

## 1. ملخص التشخيص

| # | المشكلة | الجذر | الأثر |
|---|---------|-------|-------|
| P1 | بيانات المتصفح تبقى بعد خروج المعلم | الخروج يحذف `CURRENT_USER` فقط؛ كل `zbt_*_prod_v3` يبقى | جلسات «لا تنتهي» + بيانات قديمة على كل جهاز |
| P2 | تعديلات المدير لا تصل للمعلمين | لا Firestore؛ LocalStorage لكل جهاز؛ كاش بدون إبطال | كل معلم يرى نسخة مختلفة من الجدول |
| P3 | أوقات الحصة لا تتغير للمعلم | `validatePeriodAttendance` يقرأ `INITIAL_PERIODS` وليس `SchoolSettings` | تعديل 07:45–08:30 في الإعدادات لا يؤثر على القفل |
| P4 | معرّفات المعلمين غير متطابقة | `timetableData.ts` يستخدم teacher-1..4 غير موجودين في `teachersData.ts` | إسناد الحصة الثانية لمعلمين خاطئين أو فارغ |
| P5 | أدوات التجربة في الإنتاج | `TimeSimulatorBar` افتراضي 08:15؛ محاكي المعلمين مفعّل | خلط الوقت الفعلي مع المحاكاة |
| P6 | لا مسار لرفع الجدول | `xlsx` للطلاب فقط؛ الجدول ثابت في الكود | لا يمكن رفع الجدول المحدّث من المدير |

---

## 2. أهداف الإصلاح

1. **مصدر واحد للحقيقة** للجدول وإسناد الحصة الثانية.
2. **مزامنة** تصل تعديلات المدير إلى أجهزة المعلمين خلال **< 3 ثوانٍ** (متوافق مع `AttendanceService` والوثائق).
3. **توحيد** أوقات الحصة الثانية (07:45–08:30) في التحقق والواجهة.
4. **ربط صحيح** `teacherId` بين الحسابات والجدول.
5. **عزل** أدوات التجربة عن بيئة المعلمين.
6. **مسار رفع** للجدول المحدّث (Excel) مع التحقق من المعرّفات.

---

## 3. مراحل التنفيذ

### المرحلة 0 — تحضير (قبل أي كود)

| المهمة | التفاصيل | الملفات |
|--------|----------|---------|
| 0.1 | جمع الجدول المحدّث من المدير (Excel) | خارج النظام |
| 0.2 | مطابقة كل معلم في الجدول مع `OFFICIAL_TEACHERS_LIST` (`id`, `username`, `name`) | `services/teachersData.ts` |
| 0.3 | توثيق خريطة التحويل: `timetable teacherId` → `login teacherId` | هذا الملف — قسم 4 |
| 0.4 | نسخ احتياطي LocalStorage من جهاز المدير قبل أي reset | تشغيل |

**معيار القبول:** 100% من المعلمين في الجدول لهم `teacherId` موجود في `getUsers()`.

---

### المرحلة 1 — إصلاحات حرجة (بدون مزامنة سحابية)

**الهدف:** إصلاح المنطق على نفس الجهاز؛ تقليل الارتباك فوراً.

#### 1.1 توحيد أوقات الحصة الثانية

| الإجراء | الملف |
|---------|-------|
| `getPeriods()` يقرأ من `getSettings()` لأوقات الحصة 2 أو يدمج `INITIAL_PERIODS` مع `period2StartTime/EndTime` | `services/attendanceService.ts` |
| `validatePeriodAttendance` يستخدم `settings` فعلياً (إزالة `_settings` المتجاهل) | `services/attendanceService.ts` |
| ربط `lockAttendanceOutsidePeriod` في التحقق إن كان مفعّلاً | `services/attendanceService.ts` |
| `SchoolSettingsModal` يستدعي `AttendanceService.saveSettings` عند الحفظ | `App.tsx` |

**معيار القبول:** تغيير وقت الحصة 2 في الإعدادات يغيّر رسالة القفل و`validatePeriodAttendance` فوراً.

#### 1.2 إصلاح معرّفات المعلمين في الجدول

| الإجراء | الملف |
|---------|-------|
| استبدال كل `teacherId` في `OFFICIAL_TIMETABLE_RECORDS` بمعرّفات `teachersData.ts` | `services/timetableData.ts` |
| إعادة تشغيل `extractPeriod2AssignmentsFromTimetable` بعد التحديث | `services/attendanceService.ts` |
| إضافة دالة `reseedPeriodAssignmentsFromOfficialTimetable()` مع مسح `PERIOD_ASSIGNMENTS` | `services/attendanceService.ts` |
| زر في الواجهة: «إعادة بناء إسناد الحصة 2 من الجدول المعتمد» | `Period2AssignmentScheduleTable.tsx` |

**معيار القبول:** كل `teacherId` في الإسناد يطابق `getUsers().find(id)`.

#### 1.3 إبطال الكاش والأحداث

| الإجراء | الملف |
|---------|-------|
| إضافة `SCHEDULE_CHANGE_EVENT` وإطلاقه عند `saveSettings` / `saveAllPeriodAssignments` | `services/attendanceService.ts` |
| `TeacherAttendanceSheet` يستمع للحدث ويحدّث `selectedClassId` و`periodValidation` | `components/TeacherAttendanceSheet.tsx` |
| مستمع `window.storage` لتحديث الكاش عند تغيير تبويب آخر (نفس المنشأ) | `services/attendanceService.ts` أو `App.tsx` |

**معيار القبول:** تعديل الإسناد من تبويب المدير يظهر في شاشة المعلم دون إعادة تحميل كاملة.

#### 1.4 تسجيل الخروج والجلسة

| الإجراء | الملف |
|---------|-------|
| إزالة تسجيل الدخول التلقائي للمدير/المعلم عند `CURRENT_USER === null` | `App.tsx` |
| عرض `LoginModal` إلزامياً بعد الخروج (بدون auto-login) | `App.tsx` |
| **لا** حذف بيانات المدرسة عند خروج المعلم (البيانات المشتركة تبقى حتى المرحلة 2) | — |

**معيار القبول:** بعد الخروج وتحديث الصفحة يظهر تسجيل الدخول وليس لوحة المدير.

#### 1.5 عزل أدوات التجربة

| الإجراء | الملف |
|---------|-------|
| `simulatedTime` افتراضي `null` (الوقت الفعلي) | `App.tsx` |
| إخفاء `TimeSimulatorBar` عن `role === 'teacher'` | `App.tsx` |
| `showSimulatorPanel` افتراضي `false` | `AdminDashboard.tsx` |
| إزالة أو تخفيف نصوص «تجريبي» في واجهة المعلم | متعدد |

**معيار القبول:** المعلم لا يرى محاكي الوقت؛ المدير يفتحه صراحة.

#### 1.6 إصلاح `resetToDefault`

| الإجراء | الملف |
|---------|-------|
| مسح `PERIOD_ASSIGNMENTS`, `TEACHER_REMINDERS`, `REFERRALS`, `CURRENT_USER` | `services/attendanceService.ts` |
| إعادة `_initialized = false` وإعادة تهيئة الكاش | `services/attendanceService.ts` |

---

### المرحلة 2 — مزامنة بين الأجهزة

**الهدف:** تعديل المدير يصل لجميع المعلمين.

| الخيار | الوصف | التعقيد |
|--------|-------|---------|
| **A — Firestore** | مزامنة `settings`, `period_assignments`, `users`, `classes` مع LocalStorage كنسخة | متوسط — متوافق مع AGENTS.md |
| **B — تصدير/استيراد JSON** | المدير يصدّر «حزمة الجدول»؛ المعلم يستورد من رابط/ملف | منخفض — لا خادم |
| **C — Google Sheets** | الجدول في شيت مشترك؛ polling كل 30s | موجود جزئياً في `googleSheetsService` |

**التوصية:** المرحلة 2-A (Firestore) للإنتاج؛ 2-B كحل مؤقت سريع.

**معيار القبول:** تعديل إسناد الحصة 2 على جهاز المدير يظهر على جهاز معلم آخر خلال 3 ثوانٍ.

---

### المرحلة 3 — رفع الجدول المحدّث (Excel)

| الإجراء | الملف |
|---------|-------|
| `TimetableImportModal` — استيراد Excel بصيغة محددة (معلم، يوم، حصة، فصل) | `components/TimetableImportModal.tsx` |
| التحقق: كل `teacherName` أو `nationalId` يطابق `getUsers()` | خدمة جديدة `timetableImportService.ts` |
| بعد الاستيراد: تحديث `PERIOD_ASSIGNMENTS` + (اختياري) `OFFICIAL_TIMETABLE_RECORDS` في LocalStorage | `attendanceService.ts` |
| نموذج Excel جاهز للتحميل | `public/` أو من المودال |

**معيار القبول:** رفع ملف الجدول المحدّث يعيد بناء إسناد الحصة 2 دون تعديل الكود.

---

### المرحلة 4 — تحسينات (اختياري)

- إزالة تجاوز `assignedClassId === classId` في `validatePeriodAttendance` (الاعتماد على الجدول اليومي فقط).
- `validateTeacherSessionData` يفحص `getTeacherAssignedClassForDay` وليس الفصل الثابت فقط.
- تعبئة `SchoolClass.teacherId` من الجدول عند الاستيراد.
- حل تعارضات الحصة 2 (عدة معلمين لنفس فصل/يوم) — قواعد واضحة + تنبيه للمدير.

---

## 4. خريطة تحويل معرّفات المعلمين (من التشخيص)

يجب تطبيقها عند تحديث `timetableData.ts`:

| اسم الجدول (timetableData) | teacherId الحالي في الجدول | teacherId الصحيح (teachersData) | الاسم في الحساب |
|----------------------------|----------------------------|-----------------------------------|-----------------|
| أسامة الدوغان | teacher-1 | **teacher-14** | أ. اسامة عبد اللطيف الدوغان |
| إبراهيم المحبوب | teacher-2 | **teacher-17** | أ. ابراهيم عبد الله صالح المحبوب |
| حمد المري | teacher-3 | **teacher-8** | أ. حمد محمد عبيد المري |
| خالد الملا | teacher-4 | **teacher-11** | أ. خالد محمد عمر الملا |
| خليفة القعيمي | teacher-5 | **teacher-24** | أ. خليفة سعد القعيبي |
| صالح الدوسري | teacher-6 | **teacher-5** | أ. صالح مسعود حمد الدوسري |
| عبدالرحمن الدوسري (اجتماعيات) | teacher-7 | **teacher-20** | أ. عبد الرحمن غنام الدوسري |
| فيحان المري | teacher-22 | **teacher-22** | أ. فيحان المري |

**ملاحظة:** مراجعة باقي السجلات (teacher-8..21) سطراً بسطر مع الجدول المحدّث قبل الاستيراد.

---

## 5. مفاتيح LocalStorage (مرجع)

```
zbt_users_prod_v3
zbt_classes_prod_v3
zbt_students_prod_v3
zbt_submissions_prod_v3
zbt_settings_prod_v3
zbt_period_assignments_prod_v3   ← إسناد الحصة الثانية
zbt_current_user_prod_v3         ← يُحذف فقط عند الخروج حالياً
```

---

## 6. ترتيب التنفيذ المقترح

```
المرحلة 0 (تحضير + خريطة IDs)
    ↓
المرحلة 1.2 (IDs) + 1.1 (أوقات)  ← أعلى أثر
    ↓
المرحلة 1.3 (كاش/أحداث) + 1.5 (إخفاء التجربة)
    ↓
المرحلة 1.4 (الجلسة) + 1.6 (reset)
    ↓
npm run lint && npm run build
    ↓
اختبار يدوي: مدير يعدّل → معلم يرى (نفس الجهاز)
    ↓
المرحلة 2 (مزامنة) أو 3 (رفع Excel) حسب الحاجة
```

---

## 7. اختبارات القبول (QA)

| # | السيناريو | النتيجة المتوقعة |
|---|-----------|----------------|
| T1 | مدير يغيّر معلم الحصة 2 ليوم الأحد | المعلم المعيّن يرى الفصل الصحيح |
| T2 | مدير يغيّر وقت الحصة 2 في الإعدادات | `validatePeriodAttendance` يستخدم الوقت الجديد |
| T3 | معلم يخرج ويحدّث الصفحة | يظهر تسجيل الدخول |
| T4 | معلم يفتح الشاشة خارج 07:45–08:30 (بدون محاكي) | الرصد مقفول أو تحذير واضح |
| T5 | «تثبيت من الجدول المعتمد» بعد إصلاح IDs | كل الإسنادات لمعلمين موجودين |
| T6 | جهاز معلم ثاني (بعد المرحلة 2) | يستقبل التعديل خلال 3 ثوانٍ |

---

## 8. الملفات الأساسية للتعديل

| الملف | المراحل |
|-------|---------|
| `services/attendanceService.ts` | 1.1, 1.2, 1.3, 1.6, 2 |
| `services/timetableData.ts` | 1.2, 3 |
| `services/teachersData.ts` | 0.2 |
| `App.tsx` | 1.1, 1.4, 1.5 |
| `components/TeacherAttendanceSheet.tsx` | 1.3 |
| `components/Period2AssignmentScheduleTable.tsx` | 1.2 |
| `components/TimeSimulatorBar.tsx` | 1.5 |
| `components/AdminDashboard.tsx` | 1.5 |
| `components/SchoolSettingsModal.tsx` | 1.1 |

---

## 9. ما لا يُنفَّذ في هذه الخطة

- تغيير نافذة الحصة الثانية الثابتة **07:45–08:30** (قيد المشروع).
- توليد بيانات طلاب وهمية (364 طالب من `officialStudentsData` فقط).
- كشف كلمات مرور المدير في الواجهة.

---

## 10. الموافقة على التنفيذ

| المرحلة | جاهز للتنفيذ | موافقة المدير |
|---------|--------------|---------------|
| 0 — تحضير | ☐ | ☐ |
| 1 — إصلاحات حرجة | ☐ | ☐ |
| 2 — مزامنة | ☐ | ☐ |
| 3 — رفع Excel | ☐ | ☐ |
| 4 — تحسينات | ☐ | ☐ |

**للبدء:** أرفق الجدول المحدّث (Excel) وحدّد المرحلة: «نفّذ المرحلة 1» أو «نفّذ كل المراحل».

---

*هذا المستند مبني على تشخيص الكود في `attendanceService.ts`, `App.tsx`, `timetableData.ts`, `teachersData.ts` وواجهات الإدارة والمعلم — سبتمبر 2026.*
