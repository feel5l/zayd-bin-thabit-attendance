# تسليم العمل — نظام حضور مدرسة زيد بن ثابت

**آخر تحديث:** ١٦ سبتمبر ٢٠٢٦  
**الإصدار:** v2.6.0 + مزامنة Supabase + تبسيط واجهة الإدارة  
**الإنتاج الأساسي:** https://zayd-bin-thabit-attendance.vercel.app  

هذا الملف لمشغّل المدرسة/المتسّلم التشغيلي. للتفاصيل الهندسية راجع [`ENGINEERING_HISTORY.md`](./ENGINEERING_HISTORY.md) و[`AGENTS.md`](./AGENTS.md).

> تقرير النشر: [`DEPLOYMENT_REPORT.md`](./DEPLOYMENT_REPORT.md)

---

## ١. ما يعمل الآن (مدمج ومنشور)

| الميزة | الحالة |
|--------|--------|
| رصد الحصة الثانية 07:45–08:30 | يعمل |
| مزامنة الجدول عبر Supabase | يعمل |
| دخول معلم برقم الجوال (خادمي) + توكن جهاز | يعمل |
| دخول مدير بكلمة مرور بيئية | يعمل |
| دفع الغياب `submit-attendance` + سحب `get-attendance` | يعمل |
| انعكاس الغياب على لوحة المدير (أجهزة مختلفة) | يعمل (poll ~8 ث) |
| تحديث بين تبويبات نفس المتصفح | يعمل |
| واجهة إدارة مبسّطة (تصدير/إدارة + إخفاء المحاكيات) | منشور |
| أدوات الاختبار | فقط مع `?debug=1` |

---

## ٢. المشاكل الأصلية — هل حُلّت؟

| السؤال | الجواب |
|--------|--------|
| تعديل الجدول لا يصل للمعلمين؟ | نعم عند تفعيل Supabase |
| رصد المعلم لا يظهر للمدير على جهاز آخر؟ | نعم بعد إصلاح التوكن/الدفع/السحب (انظر ENGINEERING_HISTORY §11–12) |
| الواجهة مشتتة أثناء المتابعة؟ | خُفّفت؛ الأدوات في قوائم تصدير/إدارة |

---

## ٣. روابط وبيئة

| البند | القيمة |
|-------|--------|
| الإنتاج | https://zayd-bin-thabit-attendance.vercel.app |
| Supabase | `dhpvladkiqajorowrlhj` |
| GitHub Pages (قديم/احتياطي) | كان مستخدماً سابقاً — **ليس المصدر الأساسي الآن** |

### أسرار Vercel (مضبوطة على المشروع)

- `VITE_ADMIN_PASSWORD`
- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`

أي تغيير عليها يتطلب **إعادة نشر** لأن Vite يدمجها وقت البناء.

### تطوير محلي

```bash
cp .env.example .env.local
npm install
npm run dev
```

### إعادة نشر الإنتاج

```bash
npx vercel deploy --prod --yes
```

### نشر دوال/قاعدة Supabase (عند تغييرها فقط)

```bash
npm run supabase:migrate
npm run supabase:deploy-functions
```

---

## ٤. ما لم يُنفَّذ بعد (خارطة طريق)

| البند | ملاحظة |
|-------|--------|
| OTP/SMS للمعلمين | يحتاج مزوّد مدفوع |
| بوابة Noor | مخططة |
| WhatsApp Business API | مخططة |
| بعض صفوف الجدول بلا معلم مرتبط | قرار تشغيلي |

---

## ٥. فحوصات قبل أي نشر

```bash
npm run lint
npm test          # حالياً 24 اختباراً متوقعاً
npm run build
```

تحقق مزامنة سريع بعد النشر: معلم يعتمد غياباً → لوحة المدير ترتفع من `0/11` دون تحديث يدوي.

---

## ٦. اختبار يدوي مقترح

1. افتح رابط الإنتاج على جهازين (أو متصفحين منفصلين).
2. مدير في الأول، معلم (جوال مسجّل) في الثاني.
3. إن كنت خارج 07:45–08:30 استخدم `?debug=1` واختر وقت داخل الحصة للاختبار فقط.
4. علّم غياباً واعتمد الكشف.
5. راقب لوحة المدير خلال ثوانٍ.

إن فشل: ابحث عن رسائل «مزامنة معطّلة» / «أعد تسجيل الدخول» / «غير مسند» — غالباً توكن أو إسناد فصل.

---

## ٧. قيود ثابتة

- الحصة الثانية: **07:45 – 08:30** (لا تُغيَّر)
- لا تُعرض كلمة مرور المدير في الواجهة
- بيانات الطلاب الرسمية فقط (لا بيانات وهمية)
